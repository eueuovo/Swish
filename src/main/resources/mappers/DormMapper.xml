<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="dev.dhkim.swish.mappers.DormMapper">
    <resultMap id="questionResultMap" type="dev.dhkim.swish.entities.user.DormQuestionEntity">
        <id property="id" column="question_id"/>
        <result property="content" column="question_content"/>
        <result property="order" column="question_order"/>
        <collection property="answers" ofType="dev.dhkim.swish.entities.user.DormAnswerEntity">
            <id property="id" column="answer_id"/>
            <result property="content" column="answer_content"/>
            <result property="dormId" column="dorm_id"/>
        </collection>
    </resultMap>

    <select id="selectQuestionsWithAnswers" resultMap="questionResultMap">
        SELECT
            q.`id`          AS question_id,
            q.`content`     AS question_content,
            q.`order`       AS question_order,
            a.`id`          AS answer_id,
            a.`content`     AS answer_content,
            a.`dorm_id`     AS dorm_id
        FROM `swish`.`dorm_question` q
                 LEFT JOIN `swish`.`dorm_answer` a ON q.id = a.question_id
        ORDER BY q.`order`
    </select>

    <update id="updateUserDorm">
        UPDATE user
        SET dorm_id = #{dormId}
        WHERE id = #{userId}
    </update>
</mapper>