<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="dev.dhkim.swish.mappers.ItemMapper">
    <select id="findById" resultType="dev.dhkim.swish.entities.item.ItemEntity">
        SELECT * FROM `swish`.`item`
        WHERE `id` = #{id}
    </select>

    <select id="findByUserId" resultType="dev.dhkim.swish.dto.UserItemDto">
        SELECT
            i.`id`,
            i.`name`,
            i.`description`,
            i.`image`,
            ui.`count` AS count
        FROM `swish`.`item_user` ui
                 JOIN `swish`.`item` i ON ui.`item_id` = i.`id`
        WHERE ui.`user_id` = #{userId}
    </select>
</mapper>