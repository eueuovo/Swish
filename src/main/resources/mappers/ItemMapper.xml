<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="dev.dhkim.swish.mappers.ItemMapper">
    <select id="findById" resultType="dev.dhkim.swish.entities.item.ItemEntity">
        SELECT * FROM `swish`.`item`
        WHERE `id` = #{id}
    </select>

    <select id="findByUserId" resultType="dev.dhkim.swish.dto.UserItemDto">
        SELECT
            i.`id`,
            i.`name`,
            i.`description`,
            i.`image`,
            SUM(ui.`count`) AS count
        FROM `swish`.`item_user` ui
                 JOIN `swish`.`item` i ON ui.`item_id` = i.`id`
        WHERE ui.`user_id` = #{userId}
        GROUP BY i.`id`, i.`name`, i.`description`, i.`image`
    </select>

    <select id="findAllWithUserCount" resultType="dev.dhkim.swish.dto.UserItemDto">
        SELECT
            i.`id`,
            i.`name`,
            i.`description`,
            i.`image`,
            i.`effect_type`,
            i.`effect_value`,
            COALESCE(SUM(ui.`count`), 0) AS count
        FROM `swish`.`item` i
                 LEFT JOIN `swish`.`item_user` ui ON i.`id` = ui.`item_id` AND ui.`user_id` = #{userId}
        GROUP BY i.`id`, i.`name`, i.`description`, i.`image`, i.`effect_type`, i.`effect_value`
    </select>

    <select id="countUserItem" resultType="int">
        SELECT COALESCE(SUM(`count`), 0)
        FROM `swish`.`item_user`
        WHERE `user_id` = #{userId} AND `item_id` = #{itemId}
    </select>

    <update id="decreaseItemCount">
        UPDATE `swish`.`item_user`
        SET `count` = `count` - 1
        WHERE `user_id` = #{userId} AND `item_id` = #{itemId} AND `count` > 0
    </update>
</mapper>